using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelDialogManager : MonoBehaviour
{
    /*public string path;
    public GameObject[] persons;

    private Character[] characters; // Реализовать Character
    private Character player;

    void Start()
    {
        // открываем и читаем файл и вызыывем Parse
    }

    LevelDialog Parse()
    {
        // сохраняем диалог в собств. класс LevelDialog
    }

    List<DialogData> ToDialogSystemData()
    {
        // преобразуем в классы готовой диалоговой системы
    }

    // Наследуем этот класс и добавляем свои методы для логики диалога.
    // Далее создаем объекты-триггеры, к ним присоединяем скрипт Button и
    // в него добавляем этот объект и выбираем нужный метод*/
}
